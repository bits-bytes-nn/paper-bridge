# graphrag v3 (graphrag-lexical-graph, installed by the Dockerfile) relaxed its
# botocore pin to >=1.40.61, which is compatible with bedrock-converse's aioboto3
# chain — so converse + graphrag now co-install cleanly (the old fragile
# botocore==1.35.88 clash is gone). Figure analysis no longer uses the stale
# llama-index-multi-modal-llms-bedrock package (it pins core==0.13.6, incompatible
# with v3's core==0.14.20); BedrockConverse handles images via ChatMessage
# ImageBlocks instead. The HEALTHCHECK co-imports both backends so a real break
# fails fast.
arxiv==2.1.3
llama-index-core==0.14.20
llama-index-llms-bedrock-converse==0.14.6
# graphrag v3's VectorStoreFactory.for_vector_store("aoss://...") (used by the
# retriever) loads this lazily and errors if it is missing; graphrag declares it
# as an optional extra, so pin it here. 0.6.3 supports core <0.15,>=0.13 AND
# opensearch-py<3 (graphrag pins 2.8.0; the 1.x line requires opensearch-py>=3).
llama-index-vector-stores-opensearch==0.6.3
pymupdf==1.25.3
selenium==4.29.0
webdriver-manager==4.0.2
# Direct first-party imports used by the summarizer modules.
beautifulsoup4>=4.12.0
boto3>=1.34.0
GitPython>=3.1.0
httpx>=0.27.0
Jinja2>=3.1.0
nest-asyncio>=1.6.0
opensearch-py>=2.8.0
pillow>=10.0.0
PyGithub>=2.0.0
python-dotenv>=1.0.1
requests>=2.32.0
